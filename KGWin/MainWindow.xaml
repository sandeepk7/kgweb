<Window x:Class="KGWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KGWin"
        mc:Ignorable="d"
        Title="KGWin POC App" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Navigation Button Style -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Margin="3,0">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                            </Border>
                            <!-- Underline for active state -->
                            <Border x:Name="Underline" 
                                    Background="Transparent" 
                                    Height="3" 
                                    VerticalAlignment="Bottom" 
                                    Margin="3,0,3,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#007ACC"/>
                                <Setter TargetName="Underline" Property="Background" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Active Navigation Button Style -->
        <Style x:Key="ActiveNavButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#007ACC"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Margin="3,0">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                            </Border>
                            <!-- Active underline -->
                            <Border Background="#007ACC" 
                                    Height="3" 
                                    VerticalAlignment="Bottom" 
                                    Margin="3,0,3,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Top Navigation Bar -->
        <Border Grid.Row="0" 
                Background="White" 
                BorderBrush="#E8E8E8" 
                BorderThickness="0,0,0,1"
                Padding="20,15">
            <Border.Effect>
                <DropShadowEffect Color="#CCCCCC" 
                                  Direction="270" 
                                  ShadowDepth="2" 
                                  Opacity="0.2" 
                                  BlurRadius="6"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- App Title -->
                <TextBlock Grid.Column="0" 
                           Text="KGWin POC App" 
                           FontSize="20" 
                           FontWeight="SemiBold" 
                           Foreground="#007ACC"
                           VerticalAlignment="Center"
                           Margin="0,0,25,0"/>
                
                <!-- Navigation Buttons -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center">
                    <Button x:Name="HomeButton" 
                            Content="Home" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="HomeButton_Click"/>
                    <Button x:Name="MapButton" 
                            Content="Map" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="MapButton_Click"
                            Margin="10,0,0,0"/>
                    <Button x:Name="SignalRMonitorButton" 
                            Content="SignalR Communication" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="SignalRMonitorButton_Click"
                            Margin="10,0,0,0"/>
                    <Button x:Name="BrowserExtButton" 
                            Content="Browser Ext Communication" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="BrowserExtButton_Click"
                            Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Page Content Frame -->
        <Frame x:Name="MainFrame" 
               Grid.Row="1" 
               NavigationUIVisibility="Hidden"
               Background="#FAFAFA"/>
    </Grid>
</Window>
