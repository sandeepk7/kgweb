<UserControl x:Class="KGWin.WPF.Views.Map.KGMap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
             xmlns:vm="clr-namespace:KGWin.WPF.ViewModels"
             xmlns:component="clr-namespace:KGWin.WPF.Views.Components"
             xmlns:converters="clr-namespace:KGWin.WPF.Converters"
             xmlns:local="clr-namespace:KGWin.WPF.Views.Map"
             mc:Ignorable="d" 
             x:Name="KGMapRoot"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <vm:ViewModelLocator x:Key="Locator" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <Binding Source="{StaticResource Locator}" Path="MapViewModel" />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Grid.Column="0" Visibility="{Binding MapConfig.LoadLayers, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0 0 8 0">
            <local:KGMapLayerManager DataContext="{Binding}"/>
        </Border>
        <Grid Grid.Row="0" Grid.Column="1">
            <esri:MapView 
                x:Name="KGMapView"
                Map="{Binding Map}" 
                GeoViewTapped="KGMapView_GeoViewTapped" />

            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="4 4 4 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <local:KGMapControlToolbar Grid.Column="0"/>
            </Grid>

            <component:KGPopup DataContext="{Binding KGPopupViewModel}">
                <component:KGPopup.Body>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding BodyData}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="12" FontWeight="Medium" Foreground="#6B7280" Padding="8 0 8 0"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Value}" FontSize="14" Foreground="#111827" TextAlignment="Right" HorizontalAlignment="Right" Margin="0,0,12,0" Padding="8 0 8 0"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </component:KGPopup.Body>
            </component:KGPopup>
        </Grid>
    </Grid>
</UserControl>
